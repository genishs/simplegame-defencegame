# 검수 시나리오 카탈로그 — Phase 3.5

**DECISION-TL-P3-5-005**: 사용자 검수 시나리오는 headless 자동 테스트로 대체 불가한 항목만 수록.  
자동 pytest로 커버 가능한 항목은 `regression_matrix.md` 참조.

마지막 갱신: 2026-05-19 | 기준 버전: Phase 3.5 (develop HEAD `3c8cf7f`)

---

## 검수 가이드

- **실행 환경**: Windows 11 (개발 머신 또는 릴리즈 .exe)
- **소요 시간**: 전체 약 30~45분
- **전제**: Python 3.11+, `pip install -r requirements.txt` 완료, 또는 `.exe` 더블클릭
- **결과 기록**: 각 시나리오 하단 "검수자 메모" 란에 날짜·이슈·이름 기재

---

## CAT-01: 게임 첫 실행 ~ 메인 메뉴 응답성

### 1) 사전 조건
- 저장 데이터 없는 클린 상태 (또는 `%APPDATA%\AnsiseongDefense` 폴더 삭제)
- Python 환경: `python src/main.py` 또는 .exe 더블클릭

### 2) 실행 절차
1. 게임 실행 명령어 또는 .exe 파일을 실행한다.
2. 스톱워치(또는 핸드폰 타이머)를 시작한다.
3. 메인 메뉴 화면이 나타나고 버튼 8개가 표시되는 시점에 타이머를 멈춘다.
4. 마우스 커서를 버튼 위에 올려 hover 효과(색상 변화)가 발생하는지 확인한다.
5. Up/Down 화살표 키로 포커스가 이동하는지 확인한다.

### 3) 기대 결과
- 메인 메뉴 표시까지 **3초 이내** 완료
- 타이틀 텍스트 "안시성: 88일의 약속" 가 한글로 정상 표시 (깨짐 없음)
- 버튼 hover 시 테두리 색상이 `#a88a5c` → `#d4a84a`로 변경
- 키보드 포커스 이동 시 해당 버튼 테두리 굵기가 두꺼워짐

### 4) 검수자 메모
```
날짜:
결과: PASS / FAIL
비고:
```

---

## CAT-02: Stage 1 전 구간 손맛 (영웅 M키, 스킬, 아군 배치, UI 클릭 반응)

### 1) 사전 조건
- 메인 메뉴에서 "새 게임" 또는 "스테이지" 선택 후 Stage 1 진입
- 영웅 체력 (기본 800 HP) 확인

### 2) 실행 절차
1. 메인 메뉴 → "스테이지" 버튼 클릭 → Stage 1 "요동성의 첫눈" 카드 클릭 → "도전하기"
2. 배틀 씬 진입 확인 (적 웨이브 지연 카운트다운 시작)
3. **M 키** 를 눌러 직접 조작 모드 ON 확인:
   - 화면 우측 상단 "직접 조작 모드 ON" 라벨 표시 확인
4. WASD / 방향키로 영웅을 이동시켜 이동 반응이 자연스러운지 확인
5. 적이 등장하면 영웅 자동 모드(M키 OFF)로 전환
6. Wave 1 종료 후 Wave 2 시작 확인 (카운트다운 표시)
7. **Esc 키** → 일시정지 다이얼로그 표시 확인 → "재개" 클릭
8. 모든 웨이브 소진 후 승리 화면 → "메뉴" 버튼 클릭 → 메인 메뉴 복귀

### 3) 기대 결과
- M키 토글 시 즉각적인 라벨 전환 (딜레이 없음)
- 영웅 이동: 방향키/WASD 모두 동일 속도(150 unit/s), 대각선도 동일 속도
- 적 웨이브 카운트다운 숫자가 감소하면서 표시됨
- 일시정지 다이얼로그 표시 중 게임 로직 중단 확인
- 승리 조건 충족 시 ResultDialog 표시 (재도전 / 메뉴 버튼)
- 메뉴 복귀 시 메인 메뉴 정상 표시

### 4) 검수자 메모
```
날짜:
결과: PASS / FAIL
영웅 이동 반응:
M키 반응:
일시정지 반응:
비고:
```

---

## CAT-03: .exe 더블클릭 실행 (Windows 11)

### 1) 사전 조건
- `pyinstaller AnsiseongDefense.spec` 으로 빌드된 `dist/AnsiseongDefense/AnsiseongDefense.exe` 존재
- Python 미설치 환경 또는 Python PATH 비활성화 권장 (순수 .exe 검증)

### 2) 실행 절차
1. `dist/AnsiseongDefense/` 폴더를 탐색기에서 연다.
2. `AnsiseongDefense.exe` 를 더블클릭한다.
3. Windows Defender SmartScreen 경고가 뜨면 "추가 정보" → "실행" 클릭
4. 게임이 열리는 시간을 기록한다.
5. 메인 메뉴 정상 진입 확인
6. 게임 창을 닫는다 (종료 버튼 또는 X 버튼)

### 3) 기대 결과
- 더블클릭 후 **5초 이내** 메인 메뉴 표시
- 콘솔 에러 창 팝업 없음
- 게임 창 제목 표시줄: "안시성: 88일의 약속" (한글 정상)
- 게임 종료 시 프로세스 완전 종료 (작업 관리자에서 잔여 프로세스 없음)

### 4) 검수자 메모
```
날짜:
OS: Windows 11 (버전:        )
결과: PASS / FAIL
실행 소요 시간:
SmartScreen 경고 여부:
비고:
```

---

## CAT-04: 폰트 한글 렌더링 (Malgun Gothic 미설치 환경 가정 시 Noto Sans KR 폴백)

### 1) 사전 조건
- **시나리오 A** (번들 폰트 우선): .exe 빌드 버전 사용 (Noto Sans KR OTF 번들 포함)
- **시나리오 B** (폴백 검증): Python 개발 환경에서 assets/fonts/ 디렉터리를 임시 비워두고 실행

### 2) 실행 절차

**시나리오 A — .exe 번들 폰트**
1. `dist/AnsiseongDefense/AnsiseongDefense.exe` 실행
2. 메인 메뉴에서 한글 텍스트("안시성: 88일의 약속", "새 게임", "스테이지" 등) 확인
3. 스테이지 선택 씬으로 이동하여 스테이지 이름("1. 요동성의 첫눈" 등) 확인
4. 배틀 씬 HUD의 골드/HP 등 숫자+한글 혼용 텍스트 확인

**시나리오 B — Malgun Gothic 폴백**
1. `assets/fonts/` 폴더의 `.otf` 파일을 임시 다른 위치로 이동
2. `python src/main.py` 실행
3. 한글 텍스트가 Malgun Gothic으로 표시되는지 확인 (시스템 폰트 폴백)
4. 검수 후 `.otf` 파일을 원위치

### 3) 기대 결과

**시나리오 A**
- 모든 한글 텍스트가 Noto Sans KR 계열로 매끄럽게 렌더됨
- 英문자·숫자와 한글의 자간/크기 어색하지 않음
- 정사각형 박스(□) 또는 "?" 문자 없음

**시나리오 B**
- Malgun Gothic(시스템 폰트)으로 폴백하여 한글 정상 표시
- 게임 에러 없이 실행 지속

### 4) 검수자 메모
```
날짜:
시나리오 A 결과: PASS / FAIL
시나리오 B 결과: PASS / FAIL
폰트 이상 부위 (있을 경우):
비고:
```

---

## 종합 결과 요약

| 시나리오 | ID | 결과 | 검수일 | 검수자 |
|---------|-----|------|--------|--------|
| 게임 첫 실행 ~ 메인 메뉴 응답성 | CAT-01 | — | | |
| Stage 1 전 구간 손맛 | CAT-02 | — | | |
| .exe 더블클릭 실행 (Windows 11) | CAT-03 | — | | |
| 폰트 한글 렌더링 검증 | CAT-04 | — | | |

> 모든 시나리오 PASS 시 Phase 3.5 수동 검수 완료.
> FAIL 항목은 GitHub Issue로 등록 후 `bug` + `phase3-5` 레이블 부착.
